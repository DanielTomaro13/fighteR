% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_boxingbook_boxers.R
\name{fetch_boxingbook_boxers}
\alias{fetch_boxingbook_boxers}
\title{Fetch boxers from BoxingBook API}
\usage{
fetch_boxingbook_boxers(
  page = 0,
  page_size = 50,
  sort = NULL,
  first_name = NULL,
  last_name = NULL,
  nick_name = NULL,
  quick_search = NULL,
  country = NULL,
  birth_country = NULL,
  state = NULL,
  gender = NULL,
  age = NULL,
  age_query_type = NULL,
  fights = NULL,
  fights_query_type = NULL,
  weight = NULL,
  weight_query_type = NULL,
  include_exhibitions = NULL,
  boxer_id = NULL,
  timeout = 30
)
}
\arguments{
\item{page}{Integer. Page number for pagination (default: 0).}

\item{page_size}{Integer. Number of records per page, max 100 (default: 50).}

\item{sort}{Character. Sorting criteria in format "field1,field2,ORDER"
where ORDER is ASC or DESC (default: NULL for API default).}

\item{first_name}{Character. Filter by boxer's first name (optional).}

\item{last_name}{Character. Filter by boxer's last name (optional).}

\item{nick_name}{Character. Filter by boxer's nickname (optional).}

\item{quick_search}{Character. General search across name fields (optional).}

\item{country}{Character. Filter by boxer's current country (optional).}

\item{birth_country}{Character. Filter by boxer's birth country (optional).}

\item{state}{Character. Filter by boxer's state/province (optional).}

\item{gender}{Character. Filter by gender: "male" or "female" (optional).}

\item{age}{Integer. Filter by specific age (optional).}

\item{age_query_type}{Character. Age comparison type: "EQUAL", "GREATER_THAN",
"LESS_THAN", "GREATER_THAN_OR_EQUAL", "LESS_THAN_OR_EQUAL" (optional).}

\item{fights}{Integer. Filter by number of fights (optional).}

\item{fights_query_type}{Character. Fights comparison type: same options as age_query_type (optional).}

\item{weight}{Numeric. Filter by weight in kg (optional).}

\item{weight_query_type}{Character. Weight comparison type: same options as age_query_type (optional).}

\item{include_exhibitions}{Logical. Whether to include exhibition matches in counts (optional).}

\item{boxer_id}{Integer. Filter by specific boxer ID - will return single boxer (optional).}

\item{timeout}{Numeric. Request timeout in seconds (default: 30).}
}
\value{
A data.frame containing boxer information with columns for personal
details, fight records, physical attributes, and career statistics.
Returns an empty data.frame if no boxers are found.
}
\description{
Retrieves a list of boxers from the BoxingBook API with support for
pagination, sorting, and comprehensive filtering options. This function
provides access to detailed boxer profiles including personal information,
fight records, and career statistics.
}
\details{
The function connects to the BoxingBook API to retrieve boxer data with
comprehensive filtering capabilities. It includes error handling for network
issues, API errors, and malformed responses.

The API contains over 25,000 boxer records with detailed information including:
\itemize{
\item Personal information (name, age, nationality, stance)
\item Fight statistics (wins, losses, draws, exhibitions)
\item Physical attributes (weight, calculated weight)
\item Career details (gym, verification status, activity)
}
}
\examples{
\dontrun{
# Get first 50 boxers (default)
boxers <- fetch_boxingbook_boxers()

# Search by name
ali_boxers <- fetch_boxingbook_boxers(last_name = "Ali")

# Filter by country and gender
aus_female <- fetch_boxingbook_boxers(
  country = "Australia",
  gender = "female"
)

# Active boxers with more than 10 fights
experienced <- fetch_boxingbook_boxers(
  fights = 10,
  fights_query_type = "GREATER_THAN"
)

# Quick search across all name fields
search_results <- fetch_boxingbook_boxers(quick_search = "Mike")

# Get specific boxer by ID (alternative to fetch_boxingbook_boxer)
specific_boxer <- fetch_boxingbook_boxers(boxer_id = 30794)

# Heavyweights (over 90kg)
heavyweights <- fetch_boxingbook_boxers(
  weight = 90,
  weight_query_type = "GREATER_THAN_OR_EQUAL"
)
}

}
